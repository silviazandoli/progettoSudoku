package resolutionAlgorithm

object HiddenPair2 {
/*

  def findCoppia(row: Int): (Int, Int) {
    for (i <- 0 until dimSudoku) {
    var couple = 0
    val num = matList(row)(i)

    var col1 = -1
    var col2 = -1

    for (j <- 0 until dimSudoku) {
    if (i != j) {
    if (num == matList(row)(j)) {
    couple = couple + 1

    if (couple == 1) {
    col1 = j
  } else if (couple == 2) {
    col2 = j
  }
  }
  }
  }

    if (couple == 2) {
    return (col1, col2)
  }
  }

    return (-1, -1)
  }


  def solveHiddenPair(): Unit = {

  }
*/

}